import 'package:flutter/material.dart';

class ArticleListCell extends StatelessWidget {
  const ArticleListCell({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
